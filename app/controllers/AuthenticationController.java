package controllers;

public class AuthenticationController {
    // TODO: Authenticate for a voting by voting id.
    //       If voting auth option == COOKIE || OPEN: user id should be a randomly generated uuid()
    //       If voting auth option == IP: user id should be the ip in the request
    //       Else handle external authentication: emails, or keybase
    //       In all case: return a JWT with the user id.
}
